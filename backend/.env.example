# Example environment configuration for the Flask backend
# Copy this file to `.env` and replace the placeholder values with your own.

SENDGRID_API_KEY=
# The email address that will appear in the “from” field when sending emails.
SENDER_EMAIL=
# The sender name that accompanies the `SENDER_EMAIL`. Optional.
SENDER_NAME=ทีมงานของคุณ

# Stripe payment link for the Fast‑Track option (used on the result pages).
STRIPE_LINK_4900=
# Stripe payment link for the Standard/Normal option.
STRIPE_LINK_2900=

# Secret key used by Flask for session management. You can generate one
# using: python -c "import secrets; print(secrets.token_hex(16))"
SECRET_KEY=